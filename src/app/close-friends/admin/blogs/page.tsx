import { BlogsList } from "@/components/CloseFriends";
import { Container } from "@mui/material";

export default function BlogsPage() {
	return (
		<Container maxWidth="lg">
			<BlogsList />
		</Container>
	);
}
